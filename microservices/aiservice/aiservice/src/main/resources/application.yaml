spring:
  application:
    name: aiservice
  data:
    mongodb:
      uri: mongodb://localhost:27017/aiRecommendationService
      database: aiRecommendationService


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true

server:
  port: 8083